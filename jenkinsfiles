pipeline {
    agent any
    environment {
        AWS_ACCESS_KEY_ID = credentials('aws_access_key_id')
        AWS_SECRET_ACCESS_KEY = credentials('aws_secret_access_key')

        AWS_S3_BUCKET = "artifct-repo"
        ARTIFACT_NAME = "hello-world.war"
    }
    stages {
        stage('Validate') {
            steps {
                sh "mvn validate"
                sh "mvn clean"
                
            }
        }

        stage('Build') {
            steps {
                sh "mvn compile "
            }
        }

        stage('Test') {
            steps {
                sh "mvn test"
                
            }
            post{
                always{
                    junit '**/target/surefire-reports/TEST-*.xml'
                }
            }
        }
        stage('Package') {
            steps {
                sh "mvn package"
                
            }

            post {
                success {
                 archiveArtifacts artifacts:'**/target/**.war',followSymlinks: false
                
                }
           
            }
        }
        stage('publish artifct to s3 Bucket'){
            steps {
                sh "aws configure set region us-east-1"
<<<<<<< HEAD
                sh "aws s3 cp ./target/**.war s3://$AWS_S3_BUCKET/$ARTIFACT_NAME"
=======
                sh "aws s3 cp ./target/**.war s3://$AWS_S3_BUCKET/$ARTIFCT_NAME"
                
>>>>>>> 10ea1e2ba4be1757f3cd5d2f9deaa49fe01aa34e
            }
        }
    
        stage ('deploy'){
            steps{
                echo "Last Stage"

            }
        }
    }
}